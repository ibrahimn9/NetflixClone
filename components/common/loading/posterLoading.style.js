import { StyleSheet } from "react-native";
import { SIZES, COLORS } from "../../../constants/theme";

const styles = StyleSheet.create({
  container: {
    backgroundColor: COLORS.neutral.grey_dark_1,
    height: 180,
    width: 130,
    borderRadius: 5,
  },
});

export default styles;
